class Look {
  constructor(id, dayMoment, weather, type, image) {
    this.id = id
    this.dayMoment = dayMoment
    this.weather = weather
    this.type = type  
    this.image = image
  }
}

export default Look;
